# Backend API URL — change to your backend's actual local network IP when testing on a physical device.
# Do not use localhost or 127.0.0.1 for physical mobile devices.
# Example: http://192.168.1.50:8000
EXPO_PUBLIC_API_URL=http://YOUR_BACKEND_IP:8000
